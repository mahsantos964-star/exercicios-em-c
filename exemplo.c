algoritmo "pares consecutivos"

var
   x : inteiro
   soma: inteiro

inicio

         x<-1
   enquanto x <>0  faca
   escreval("digite um numero")
   leia(x)

    se x =0  entao
    senao
    se x mod 2 = 0 entao
       soma<- x + x+2+ x+4 + x+6 + x+8
        escreval(" soma = " ,soma )
      senao
      x<- x +1
      soma<- x + x+2+ x+4 + x+6 + x+8
        escreval(" soma = " ,soma )


   fimse
    fimse




   fimenquanto





fimalgoritmo