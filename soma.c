#include <stdio.h>

int main()
{

    int x, y, soma;

    printf("Digite um numero inteiro\n");
    scanf("%i", &x);

    printf("Digite um numero inteiro\n");
    scanf("%i", &y);

    soma = x + y;

    printf("o valor da soma e = %i", soma);

    return 0;
}